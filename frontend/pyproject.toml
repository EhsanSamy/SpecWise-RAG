[project]
name = "specwise-frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "EhsanSamy", email = "ehsansamy9@gmail.com" }
]
requires-python = ">=3.11,<3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "streamlit>=1.63.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
include = ["app.py", "api_client.py"]